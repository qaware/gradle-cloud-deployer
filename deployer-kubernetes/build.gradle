jar {
    baseName = 'deployer-kubernetes'
    version = "$version"
}

dependencies {
    compile project(':deployer-commons')
    compile 'com.fasterxml.jackson.dataformat:jackson-dataformat-yaml:2.8.1'

    testCompile 'io.fabric8:kubernetes-client:1.4.4'
}
